TELEGRAM_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here

# Host port mappings (optional)
API_PORT=8055
FRONTEND_PORT=8081

# Frontend API configuration
# For production, set this to your actual API server URL (e.g., https://vm18.digisoov.ee:8055)
# For development, leave as default or set to http://localhost:8055
VITE_API_BASE_URL=https://vm18.digisoov.ee:8055

# CORS Configuration (REQUIRED for production)
# Comma-separated list of allowed origins for API CORS
# IMPORTANT: Set this explicitly in production! Do not rely on defaults.
# Format: "https://your-domain.com,https://www.your-domain.com"
# For production, only include your actual frontend domain(s)
CORS_ALLOWED_ORIGINS=https://vm18.digisoov.ee

# Optional: override default DB connection used by API
# DB_URL=postgresql://postgres:postgres@db:5432/postgres

# Bot language setting (ru or en)
BOT_LANGUAGE=ru

# Geocoding API key for location conversion
GEOCODING_API_KEY=your_geocoding_api_key_here

# Frontend password for authentication
FRONTEND_PASSWD=frontend

# Birthday Functionality
# Set to "Yes" to enable birthday greetings and /check_birthdays command
# Default: No
# Note: Birthday data is still collected during onboarding regardless of this setting
BIRTHDAYS=No

# Group Configuration
# TELEGRAM_GROUP_ID - The group where the bot operates
# TELEGRAM_GROUP_ID=-1001234567890

# Optional: topic gating for Telegram supergroups with topics
# Set the topic/thread ID where thanks-related commands are allowed
# THANKS_TOPIC_ID=12345
# Set the topic/thread ID where birthday-related commands are allowed (requires BIRTHDAYS=Yes)
# BIRTHDAY_TOPIC_ID=67890

# Optional: configure which commands should only work in direct messages
# Comma-separated list of command names (without /)
DM_ONLY_COMMANDS=edit_profile,view_profile,my_matches,browse,report_an_issue,suggest_a_feature

# TMA (Telegram Mini App) URL — HTTPS required by Telegram
# Set to your public domain, e.g. https://vm18.digisoov.ee:8082
# Used to generate "View Profile" buttons in match notifications
TMA_URL=https://vm18.digisoov.ee:8082

# TMA service port
TMA_PORT=8082
